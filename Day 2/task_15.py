# Summing a Million
# Create the range of numbers from 1 to 1,000,000
numbers = range(1, 1_000_001)

# Confirm the smallest number
print(f"Minimum: {min(numbers)}")

# Confirm the largest number
print(f"Maximum: {max(numbers)}")

# Sum all numbers
print(f"Sum: {sum(numbers)}")